# UDC2025-3

Saitama AED Locator (Night-Adaptive) demo.

## Run

Open `index.html` in a browser (local static hosting recommended for CSV fetch).

If you need a quick local server:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
